{% extends 'base.html.twig' %}

{% block body %}
    <h1>Update</h1>
    <div class="form-admin">
        {{ form_start(form) }}
            {{ form_rest(form) }}
            <button class="admin-update" type="submit">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}